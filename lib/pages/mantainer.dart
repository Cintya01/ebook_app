import 'package:flutter/material.dart';

class Mantainer extends StatelessWidget {
  const Mantainer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Mantainer Page'),
      ),
    );
  }
}
